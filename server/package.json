{
  "name": "home-test",
  "version": "1.0.0",
  "description": "Home test assignment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "nodemon server.js",
    "test:watch": "nodemon --exec 'mocha __test__/**/*.js'",
    "test": "mocha __test__/**/*.js --exit",
    "test:coverage": "nyc --reporter=text npm run test"
  },
  "author": "nur",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^5.2.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.2.0",
    "nodemon": "^2.0.4",
    "nyc": "^15.1.0",
    "supertest": "^4.0.2"
  }
}
